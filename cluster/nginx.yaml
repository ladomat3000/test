apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nginx
  namespace: default
spec:
  chart:
    chart: nginx
    version: ">=15.0.0"
  sourceRef:
    kind: HelmRepository
    name: bitnami
  interval: 10m
  releaseName: nginx
  values:
    replicaCount: 3
